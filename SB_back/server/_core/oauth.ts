import type { Express } from "express";

export function registerOAuthRoutes(app: Express) {
  // OAuth routes disabled - using email/password authentication instead
  console.log("[OAuth] OAuth routes disabled - using email/password auth");
}
